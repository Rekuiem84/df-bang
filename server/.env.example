# Port d'écoute (Render fournit automatiquement PORT)
PORT=3001

# Origine autorisée pour CORS / Socket.io (URL du frontend Vercel en prod)
# En dev, laisser * ou http://localhost:5173
CLIENT_ORIGIN=*
